#ifndef WINDOW_H_INCLUDED
#define WINDOW_H_INCLUDED

#include <string>

#include <SDL2/SDL.h>

#include "mat.h"
/*
class Window
{
    using HandleType = SDL_Window *;
    struct HandleParam
    {
        std::string name;
        ivec2 pos;
        ivec2 size;
        uint32_t flags;
    };
    HandleType HandleCreate(const HandleParam &param)
    {
        return SDL_CreateWidnwo
    }
};*/

#endif
